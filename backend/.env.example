# Copy to .env and fill in secrets before first run.
# cp backend/.env.example backend/.env

# ── Auth ─────────────────────────────────────────────────────
HF_TOKEN=hf_...

# ── Cache paths (host absolute paths) ────────────────────────
CACHE_QWEN_VL=/home/ol1n/deploy/AiStack/cache/qwen-vl

# ── OCR API — fallback Python OCR (Qwen2.5-VL) ───────────────
# Primární OCR je Nemotron NIM z AiStack (docker-compose.ocr.yaml).
OCR_MODEL_ID=Qwen/Qwen2.5-VL-7B-Instruct
